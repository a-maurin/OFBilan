# Profil par défaut - Sites inscrits ou classés
schema_version: 2
id: sites_inscrits_classes
label: "Sites inscrits ou classés (SNC 5.3)"
out_subdir: bilan_sites_inscrits_classes

filter:
  type: keywords
  keywords: ["sites inscrits", "sites classés", "sites classes", "SNC 5.3"]
  columns: [theme, type_actio, nom_dossie]

natinf_pve: []
natinf_pej: []

analyse_PVe: true

sources:
  point_ctrl: true
  pej: true
  pa: true
  pve: true

periode_analyse:
  ventilation:
    type: auto
    seuil_jours: 366

options:
  cartes:
    label: "Intégration des cartes"
    default: true
    ask: false
